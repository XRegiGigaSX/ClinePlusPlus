created .venv 
    python -m venv .venv
    .venv\Scripts\activate
created postgres db using "psql -U postgres -h localhost -c "CREATE DATABASE code_indexer;"
started neo4j db manually
created uvicorn webpage, run it using "uvicorn code_indexer.api:app --host 0.0.0.0 --port 8000 --reload"
check sqlite db listed repository_registry on /api/repositories
download grammar for languages in grammar folder
    git clone https://github.com/tree-sitter/tree-sitter-python.git
    git clone https://github.com/tree-sitter/tree-sitter-java.git
    git clone https://github.com/fwcd/tree-sitter-kotlin.git
    git clone https://github.com/tree-sitter/tree-sitter-cpp.git